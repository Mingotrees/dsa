#include <stdio.h>
#include <stdlib.h>

typedef struct curry{
    int node;
    struct node* next;
    struct node* prev;
}*LIST;


void insert(LIST* A);
void delete(LIST* A);
void display(LIST A);
void init(LIST *A);

int main(){

}

void init(LIST *A){
    
}
void insert(LIST* A);
void delete(LIST* A);
void display(LIST A);